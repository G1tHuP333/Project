{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start-app",
      "type": "shell",
      "command": "echo -e '\\033[0;35mBooting up workspace...\\033[0m'; if test -e start.sh; then echo 'Located start.sh – running sequence'; while ! bash start.sh; do echo 'Run failed. Re-attempt in 3 seconds...'; sleep 3; done; echo 'start.sh ran successfully.'; else echo 'No start.sh file present. Process bypassed.'; fi",
      "problemMatcher": [],
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
